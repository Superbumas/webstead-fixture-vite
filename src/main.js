document.querySelector("#app").innerHTML = `
  <h1>Webstead Vite fixture ✓</h1>
  <p>Built with <code>vite build</code> → <code>dist/</code>. If this renders on ICP, the Docker build import path works.</p>
`;
